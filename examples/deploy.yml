config:
    theme/name: custom-theme
    mu-plugin/name: core-functionality
    local_url: http://test.local
hosts:
    test.dev:
        labels:
            stage: staging
        public_url: https://test.dev
        deploy_path: ~/httpdocs/
        log_path: ~/releases
        release_path: "{{deploy_path}}"
        document_root: "{{release_path}}"
        dump_path: ~/data/dumps
        backup_path: ~/data/backups
        hostname: 123.123.123.123
        port: 22
        remote_user: test_ndburhkmkyi
